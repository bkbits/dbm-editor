/**
 * AI 层缺省值：跨层共享的常量（种子层 mock/db.ts 与设置页 AiSettingsSection.vue
 * 共同引用，避免默认文本多处硬拷贝漂移）。
 */

/**
 * 全局规则默认文本（AI 设置）。
 *
 * - 种子库（mock/db.ts createSeedDB）以它为初值：新库开箱即带任务流程约定；
 * - 设置页「恢复默认」按钮以它为重置目标：清空或改坏后可一键找回；
 * - 已保存的旧库值不受影响（含用户主动清空的空串——留空表示仅使用内置默认规则）。
 */
export const DEFAULT_AI_GLOBAL_RULES = `# 术语
- 元素：表分类、表、字典分类、字典、导航、整体设置
- 表位置: 表卡片在画布中的坐标，通常表卡片未展开时宽度最大为270px

# 任务流程:
1. 读取设置，避免给予脏数据执行任务
2. 执行刷新，重新加载模型，避免给予脏数据执行任务
3. 分析任务需求，如果有不明确的部分提供多种方案交给用户选择
4. 指定任务队列清单
5. 按任务队列清单执行任务
6. 执行刷新，重新加载模型，避免给予脏数据进行任务校验
7. 校验任务执行结果
8. 输出任务报告

# 遵守规则
- 不要一次加载所有技能，应该根据需要层层递进的加载
- 不要一次性更新所有元素，一轮工具调用只执行一个元素的更新
- 不要一次性添加大量元素，一轮问答只负责添加一个元素
- 如果是更新表位置，那么尽量在一轮问答完成所有位置更新
- 每一阶段任务完成必须更新任务队列清单
- 任务报告必须包含任务完成情况，未完成原因。
`;
